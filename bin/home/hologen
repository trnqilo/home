#!/usr/bin/env bash
set -e

mkdir -p "$HOLO"
cd "$HOLO"
if [[ ! -d .git ]]; then
  git init -b main
  git add -A
  git commit -m"initialize home"
  if [[ "$1" ]]; then
    git remote add origin "$1" && git push origin main -u
  fi
fi
