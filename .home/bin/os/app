#!/usr/bin/env bash
set -e

if whereami darwin; then
    if [[ "$1" == 'chro' ]]; then app=google\ chrome
  elif [[ "$1" == 'dis' ]]; then app=discord
  elif [[ "$1" == 'disks' ]]; then app=disk\ utility
  elif [[ "$1" == 'dock' ]]; then app=docker\ desktop
  elif [[ "$1" == 'edge' ]]; then app=microsoft\ edge
  elif [[ "$1" == 'excel' ]]; then app=microsoft\ excel
  elif [[ "$1" == 'ff' ]]; then app=firefox
  elif [[ "$1" == 'keys' ]]; then app=keychain\ access
  elif [[ "$1" == 'mon' ]]; then app=activity\ monitor
  elif [[ "$1" == 'prefs' ]]; then app=system\ settings
  elif [[ "$1" == 'pm' ]]; then app=postman
  elif [[ "$1" == 'rdp' ]]; then app=windows\ app
  elif [[ "$1" == 'saf' ]]; then app=safari
  elif [[ "$1" == 'term' ]]; then app=terminal
  elif [[ "$1" == 'vbox' ]]; then app=virtualbox
  elif [[ "$1" == 'word' ]]; then app=microsoft\ word
  else app="$@"; fi
  open -a "$app"
fi

# todo find apps for `wherami`
# set -euo pipefail

# dirs=(
#   "/Applications"
#   "$HOME/Applications"
#   "/System/Library/CoreServices"
# )

# for dir in "${dirs[@]}"; do
#   if [ -d "$dir" ]; then
#     find "$dir" -type d -name "*.app" -prune -print 2>/dev/null | LC_ALL=C sort -f
#   fi
# done
