#!/usr/bin/env bash
set -e

# pstate_perf='/sys/devices/system/cpu/intel_pstate/max_perf_pct'

# [[ "$1" ]] && echo "$1" | sudo tee "$pstate_perf" && sleep 2

# cat "$pstate_perf"
grep 'MHz' '/proc/cpuinfo'
