#!/usr/bin/env bash
set -o pipefail

if whereami windows; then ping -n 1 -w 1000 "$@" 2>/dev/null
else ping -c 1 -W 1 "$@" 2>/dev/null; fi \
  | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' \
  | head -n 1

  # | $awk '{print $3}' | $sed 's/(//g;s/)//g;s/://g'
