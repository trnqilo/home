#!/usr/bin/env bash
set -e -o pipefail

if [[ "$1" ]]; then prompt+="$@"; fi
if [[ ! -t 0 ]]; then prompt+=$(cat); fi

if [[ "$prompt" ]]; then
  claude --dangerously-skip-permissions -p <<< "$prompt"
else
  echo 'no prompt provided'; exit 1
fi
